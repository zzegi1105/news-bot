name: 종합 경제 뉴스 알림 (오전 8시)

on:
  schedule:
    # KST 오전 8시 = UTC 전날 23시 (월~금)
    - cron: '0 23 * * 1-5'
  workflow_dispatch:  # 수동 실행 버튼

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true  # Node.js 24 즉시 사용

jobs:
  send-news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5  # 최신 버전

      - name: Set up Python
        uses: actions/setup-python@v6  # 최신 버전
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run comprehensive economic news
        env:
          DISCORD_WEBHOOK_1: ${{ secrets.DISCORD_WEBHOOK_1 }}
          DISCORD_WEBHOOK_2: ${{ secrets.DISCORD_WEBHOOK_2 }}
        run: |
          python news.py
